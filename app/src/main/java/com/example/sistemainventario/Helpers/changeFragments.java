package com.example.sistemainventario.Helpers;

import android.app.Activity;
import android.content.Context;

import androidx.fragment.app.Fragment;

import com.example.sistemainventario.R;

public class changeFragments {

    public void changeFragment(Context context, Fragment fram) {

        /*getActivity().getSupportFragmentManager().beginTransaction()
                .setCustomAnimations(android.R.animator.fade_in, android.R.animator.fade_out)
                .addToBackStack(null).replace(R.id.frContenido, fram).commit();*/
    }
}
